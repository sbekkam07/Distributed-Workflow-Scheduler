# Project Agent Instructions

Read [.agents/AGENTS.md](.agents/AGENTS.md) before making changes. It defines
the Distributed Workflow Scheduler's architecture, phase boundaries, safety
rules, and current implementation status.

Use a matching focused brief from `.agents/` for reviews involving distributed
systems, PostgreSQL, tests, or the API/worker boundary. The project must remain
within the user-requested phase.
