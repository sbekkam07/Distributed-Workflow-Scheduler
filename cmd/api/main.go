// Command api will expose the workflow scheduler's HTTP API.
//
// In Phase 1 it will own the POST /jobs and GET /jobs/{id} endpoints.
package main

import "fmt"

func main() {
	fmt.Println("API service is not configured yet")
}
